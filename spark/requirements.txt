# hadoop-aws, aws-java-sdk-bundle, and the postgresql JDBC driver are JVM
# jars fetched directly into $SPARK_HOME/jars/ in spark/Dockerfile — they
# are not pip packages and do not belong in this file.

pyspark==3.5.1
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5
hvac==2.3.0
prometheus-client==0.20.0
boto3==1.34.131
psycopg2-binary==2.9.9
