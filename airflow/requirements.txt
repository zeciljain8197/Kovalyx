apache-airflow==2.9.3
apache-airflow-providers-apache-spark==4.7.1
hvac==2.3.0
psycopg2-binary==2.9.9
dbt-core==1.8.2
dbt-postgres==1.8.2
# Added: the DAG's BashOperator tasks (kafka_producer_trigger,
# bronze_kafka_consumer, bronze_batch_ingest, silver_ge_validation,
# silver_to_postgres_loader) shell out to ingestion/, scripts/, and
# quality/ scripts directly inside this container — none of their
# dependencies were installed here before, so those tasks failed
# immediately with ModuleNotFoundError (masked as "success" on the two
# tasks whose bash_command ends in `|| true`).
confluent-kafka==2.4.0
faker==25.8.0
python-dotenv==1.0.1
prometheus-client==0.20.0
boto3==1.34.131
python-json-logger==2.0.7
jsonschema==4.22.0
pandas==2.2.2
pyarrow==16.1.0
great-expectations==0.18.19
# apache-airflow==2.9.3 pins sqlalchemy<2.0,>=1.4.36 — silver_to_postgres_loader.py's
# create_engine()/text() calls are 1.4-compatible.
sqlalchemy==1.4.52
# silver_pyspark_transform's spark-submit runs in --deploy-mode client,
# so the Spark driver process (and therefore silver_transform.py/
# pii_masking.py's own top-level imports) executes inside this
# container, not just on the spark-worker executors — same class of
# gap as the packages added above. NOT adding pyspark itself here: the
# real spark-submit binary (full Spark distribution installed in this
# Dockerfile) puts its own bundled Python API on PYTHONPATH ahead of
# site-packages, so a pip-installed pyspark would be both unused and a
# needless version conflict with the pyspark apache-airflow-providers-
# apache-spark already pulls in transitively.
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5
